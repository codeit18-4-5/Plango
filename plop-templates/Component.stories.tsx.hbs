import type { Meta, StoryObj } from "@storybook/react";
import { {{pascalCase name}} } from "@/components/ui";

const meta = {
  title: "UI/{{pascalCase name}}",
  component: {{pascalCase name}},
  tags: ["autodocs"],
  parameters: {
    docs: {
      description: {
        component: "{{pascalCase name}} 컴포넌트",
      },
    },
  },
  argTypes: {},
} satisfies Meta<typeof {{pascalCase name}}>;

export default meta;

type Story = StoryObj<typeof {{pascalCase name}}>;

export const Primary: Story = {
  args: {},
};